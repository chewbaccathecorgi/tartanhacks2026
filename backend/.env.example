# Copy to .env and set values. Do NOT commit .env or real secrets.
# Deployment: set these in your host (e.g. Railway, Render, Fly.io, or server env).

# Required: Postgres connection string (any host, e.g. Neon, Supabase, RDS)
# Supabase: Project Settings → Database → Connection string (URI)
#   - Migrations: use Direct connection (port 5432); pooler may not support CREATE EXTENSION
#   - App runtime: Transaction pooler (port 6543) or Direct both work
#   - SSL is auto-appended for Supabase if missing
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Vector dimensions; must match migrated schema (defaults: 512, 1536)
# FACE_EMBEDDING_DIM=512
# TEXT_EMBEDDING_DIM=1536

# Server (deployment: bind to all interfaces, use process PORT)
HOST=0.0.0.0
PORT=8000

# CORS: comma-separated origins, or * for allow-all
# Example: https://your-app.vercel.app,https://admin.example.com
CORS_ORIGINS=*

# Optional
# APP_ENV=production
# LOG_LEVEL=info
