# Azure Face API — required for face detection and processor on the server.
# Get from Azure Portal: Face resource (Cognitive Services) → Keys and Endpoint.
# Copy the Endpoint URL and Key 1 (or Key 2) — do not commit real values; set them in
# Azure App Service → Configuration → Application settings (AZURE_FACE_ENDPOINT, AZURE_FACE_KEY).
# See AZURE_SETUP.md for exact steps.
AZURE_FACE_ENDPOINT=
AZURE_FACE_KEY=

# Optional: override listen port (Azure sets PORT automatically; local default 3001)
# PORT=3001
