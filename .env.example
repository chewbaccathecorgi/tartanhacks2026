# Azure Face API — optional; used for face detection and profile deduplication on the server.
# Get from Azure Portal: Face resource (Cognitive Services) → Keys and Endpoint.
# Set in .env.local; do not commit real values. See AZURE_SETUP.md.
AZURE_FACE_ENDPOINT=
AZURE_FACE_KEY=

# Optional: override listen port (default 3001)
# PORT=3001

# When using ngrok, set these so script/asset URLs load correctly (restart dev server after changing):
# NEXT_PUBLIC_APP_URL=https://postamniotic-unstuttering-messiah.ngrok-free.dev
# NEXT_HOSTNAME=postamniotic-unstuttering-messiah.ngrok-free.dev
